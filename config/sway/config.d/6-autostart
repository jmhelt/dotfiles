# Autostart
# Terminal Server
exec foot -s

# Notification
exec mako

# Spotify with wayland support
exec spotify --enable-features=UseOzonePlatform --ozone-platform=wayland

# Move cursor to the first workspace
exec sleep 1 && swaymsg "workspace $ws1"
